[project]
name = "lastunit"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic-settings",
    "uvicorn[standard]>=0.52.4",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
