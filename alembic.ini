[alembic]
script_location = alembic
prepend_sys_path = .
path_separator = os
# The async env.py reads the URL from app.config.Settings (DATABASE_URL env),
# not from here — this stays empty.
sqlalchemy.url =
