# Node floor in package.json engines is enforced, not advisory: npm otherwise
# only warns, and a stale Node fails later with confusing errors (eslint
# 10 needs Array.prototype.findLastIndex, vitest 5 needs node:util styleText).
engine-strict=true
