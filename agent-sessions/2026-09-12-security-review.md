# 2026-09-12 — security-review по диапазону `efdaea8..HEAD`

Инструмент: Claude Code (CLI). Модель: Claude Fable 5.1 (Anthropic). Полный
экспорт — `2026-09-12-security-review.jsonl`.

Сессия 10:30–10:58 UTC, сразу после fix-first-прохода: прогнать тот же разбор,
но уже как security-review, по коммитам `efdaea8..HEAD`.

## Метод

Три параллельных поиска по зонам (auth и API-роутеры; платежи, вебхук, paystub,
планировщик, инфраструктура; фронтенд и SSE), затем отдельный фильтр ложных
срабатываний на каждого кандидата с порогом 8/10. Тесты, `*.md` и
`agent-sessions/` вне области; DoS, rate limiting и хранение секретов на диске
исключены правилами скилла.

## Результат

**Ни одного HIGH или MEDIUM.** Шесть кандидатов уровня Low, каждый отброшен
фильтром с оценкой 2/10 (заглушка paystub по дизайну, SSE-поток не закрывался
при logout, `APP_ENV=dev` и cookie без `Secure`, 409 на занятый email, пароль
без `min_length`, `provider_ref` в URL в логах httpx).

Оформлено разделом «Security review» в конце
`docs/code-audit-2026-09-12.md` — с таблицей кандидатов и списком проверенного
(сессии, авторизация, платежи, инъекции, секреты и сборка, seed и prod-guard).

## Примечание

**Коммита в этой сессии нет** — правка осталась в рабочем дереве. Она была
закоммичена позже, в `ab0b9c7`, вместе с поправкой: скоуп `order_status` был
записан здесь как чистая победа, хотя имел функциональное последствие —
витрина магазина перестала видеть оплаты. Подробности в
`2026-09-12-ci-repair-and-hardening`.
