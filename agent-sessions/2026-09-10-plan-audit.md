# 2026-09-10 — независимый аудит плана, ревизия плана, лог решений

Инструмент: Claude Code (CLI). Модель: Claude Fable 5.1 (`claude-fable-5-1`).
Полный экспорт: `2026-09-10-plan-audit.jsonl` (снимок на закрытии сессии;
сессия прерывалась по сети и была продолжена, экспорт содержит обе части).
Email пользователя в экспорте заменён на `[user email]`.

## Что сделано (по времени, MSK)
- 15:58–16:20 — `/plan`: аудит `docs/plan.md` как «не автор плана» по чеклисту
  пользователя (полнота, инварианты, время, порядок коммитов, объём, неявные
  решения). Прочитаны план, ТЗ, правила, Makefile, CI, compose, Dockerfile,
  vite.config.ts. Десять находок: rollback-на-тест ломает тесты на гонки;
  порядок шагов в `/pay` не зафиксирован; нет backend-коммита для экрана
  магазина и кабинета; `make up` не применяет миграции; коммит 13 зависит от
  14; порог покрытия сломает `make check` на фронтенд-коммитах; тест 1 помечен
  unit, а гарантия в SQL; `SECRET_KEY` без потребителя и неверное утверждение
  про Ruff DTZ; машины состояний не определены; SSE через httpx ASGITransport
  не читается. Семь вопросов человеку.
- 16:20–16:40 — ответы пользователя на семь вопросов; отчёт с ответами
  сохранён в `docs/plan-audit-2026-09-10.md`.
- 16:40–16:57 — первая ревизия `docs/plan.md` по находкам. Обрыв соединения.
- 17:00–17:18 — продолжение: уточнённые указания пользователя (платежи как
  попытки, одна `pending` одновременно; отклонённая попытка оставляет
  резервацию и заказ; вместо периодической сверки — повтор вебхука в заглушке
  и кнопка «проверить статус» через обработчик вебхука; SSE через broadcaster
  и генератор плюс один smoke на uvicorn; сессии — `token_urlsafe(32)`,
  SHA-256 в базе, HttpOnly + SameSite=Lax; argon2id через pwdlib). План
  переписан, что диктуют модули правил — ссылками, не пересказом. Четырнадцать
  датированных записей в `DECISIONS.md`. В `docs/acceptance.md` добавлена
  строка об источнике; десятого пункта «Ожидаемого поведения» в оригинале нет
  (проверено по экспорту первой сессии — девять маркеров), нумерация оставлена.
- 17:18 — коммит `9d23985`, 17:22 — пуш десяти коммитов в `origin/main`.

## Коммиты сессии
- `9d23985` docs: plan revised after audit, initial decisions
- следующий коммит: docs: session export and log for the plan audit

## Дальше
Этап 0, коммит 1 по `docs/plan.md`: пакет `app/` с `create_app(clock, broadcaster)`,
Settings только с `APP_ENV` и `LOG_LEVEL`, `/api/health`, dev-зависимости,
`test_config_env_contract`, `test_no_local_time`. Локально нужен Node 24.
